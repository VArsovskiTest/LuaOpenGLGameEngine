import { FileLoaderDirective } from './file-loader-directive';

describe('FileLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FileLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
